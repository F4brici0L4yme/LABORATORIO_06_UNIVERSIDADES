1 Crear la imagen

docker build -t imgs .
 
2 Crear el contenedor en base a la imagen

docker run -d --name pweb1 -p 8099:80 imgs
 
Docker exec -it pweb1 bash

mysql -u root -p
use puml_history;
select * from users;
select * from files;